import { EmptyState } from "@/components/ui/empty-state";

export function CalculatorShell() {
  return (
    <EmptyState
      title="Price calculator"
      description="The calculator will collect project inputs and return estimates from the backend when that service is connected."
    />
  );
}
